# Built place / model files (generated from source via Rojo)
/brookhaven.rbxlx
/build.rbxlx
*.rbxl
*.rbxm

# Roblox Studio lock files
/*.rbxlx.lock
/*.rbxl.lock

# Generated tooling artifacts
sourcemap.json
roblox.yml

# Wally packages (installed via `wally install`; commit wally.lock instead)
/Packages/

# Secrets — Open Cloud API key
.env

# Build output
/build/

# macOS
.DS_Store

# OpenACP local workspace (SECRETS: bot token, api keys, private media)
.openacp/
